"""Scoring service -- Stage 2 deep-scoring graph for the feed pivot.

Phase 0 ships only the rubric prompts (`prompts.py`); the graph builder, dimension nodes,
and `ScoringContext` are Phase 1. See `ARX-55` and
`ARX-56`.
"""
