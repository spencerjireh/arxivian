meta {
  name: List Tasks
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/v1/tasks/?limit=20&offset=0
  body: none
  auth: none
}

params:query {
  limit: 20
  offset: 0
}

docs {
  List background tasks for the current user with pagination.

  Query parameters:
  - limit: Maximum number to return, 1-100 (default: 20)
  - offset: Number of tasks to skip (default: 0)

  Requires authentication.
}
