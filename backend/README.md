# Arxivian backend

FastAPI + Celery + LangGraph service for the Arxivian feed. Layout, commands, conventions
and gotchas are in the repository root `AGENTS.md`; design docs live in Plane (`ARX-55` pipeline, `ARX-56` rubric).
This file exists because `pyproject.toml` declares it as the package readme.
